name: firebase_rest_workflow_ci_firestore_test
description: Workflow support to run the firestore_rest env test only
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  path: ">=1.9.1"
  process_run: ">=1.3.4"
  lints: ">=6.1.0"
  tekartik_lints:
    git:
      url: https://github.com/tekartik/common.dart
      path: packages/lints
    version: '>=0.1.0'
resolution: workspace
